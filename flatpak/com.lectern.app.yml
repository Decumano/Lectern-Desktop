id: com.lectern.app
runtime: org.gnome.Platform
runtime-version: '47'
sdk: org.gnome.Sdk
command: lectern

finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --share=ipc
  # Cloud sync (src-tauri/src/cloud.rs)
  - --share=network
  # Required: the folder picker is rfd's GTK3 backend, not an XDG portal, so it
  # can only show paths mounted into the sandbox. Use --filesystem=host if
  # workspaces may live outside $HOME.
  - --filesystem=home

modules:
  - name: lectern
    buildsystem: simple
    sources:
      - type: file
        path: com.lectern.app.metainfo.xml
      - type: file
        url: https://github.com/Decumano/Lectern-Desktop/releases/download/v0.12.11/Lectern_0.12.11_amd64.deb
        sha256: b8b31d52a65b32a71ea176d36b8dfecfebdab76ce1d85ed8ffd9619584dec0a7
        only-arches: [x86_64]
    build-commands:
      - mkdir deb-extract
      - ar -x *.deb --output deb-extract
      - tar -C deb-extract -xf deb-extract/data.tar.gz
      - install -Dm755 deb-extract/usr/bin/lectern /app/bin/lectern
      - sed -i 's/^Icon=.*/Icon=com.lectern.app/' deb-extract/usr/share/applications/Lectern.desktop
      - sed -i 's/^Categories=.*/Categories=Office;/' deb-extract/usr/share/applications/Lectern.desktop
      - install -Dm644 deb-extract/usr/share/applications/Lectern.desktop /app/share/applications/com.lectern.app.desktop
      - install -Dm644 deb-extract/usr/share/icons/hicolor/32x32/apps/lectern.png /app/share/icons/hicolor/32x32/apps/com.lectern.app.png
      - install -Dm644 deb-extract/usr/share/icons/hicolor/128x128/apps/lectern.png /app/share/icons/hicolor/128x128/apps/com.lectern.app.png
      - install -Dm644 deb-extract/usr/share/icons/hicolor/256x256@2/apps/lectern.png /app/share/icons/hicolor/256x256@2/apps/com.lectern.app.png
      - install -Dm644 com.lectern.app.metainfo.xml /app/share/metainfo/com.lectern.app.metainfo.xml
